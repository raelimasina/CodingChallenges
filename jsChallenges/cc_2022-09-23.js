function makeNegative(num) {
    //check if num is already negative
    //if the num is not negative, multiply by -1 and return it
    //if the num is negative, return it
    num = -Math.abs(num);
    return num;
  }